[project]
name = "range-strategy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ultimate-setups",
]

[tool.uv.sources]
ultimate-setups = { path = "../ultimate_setups", editable = true }
